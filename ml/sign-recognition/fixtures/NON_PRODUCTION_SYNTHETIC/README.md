# NON-PRODUCTION synthetic fixtures

Nothing here represents a human language or a real signer. The generator
creates deterministic numeric class patterns only for exercising loading,
signer-grouped splitting, training, metrics, export, and runtime parity.

Generated `.npz` files are ignored by Git and must not be used in product
evaluation, demos, model cards, or promotion decisions.
